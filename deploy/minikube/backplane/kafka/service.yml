apiVersion: v1
kind: Service
metadata:
  name: backplane-kafka
  namespace: cecochat
  labels:
    app: backplane-kafka
spec:
  selector:
    app: backplane-kafka
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 9092
