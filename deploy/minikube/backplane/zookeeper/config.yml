apiVersion: v1
kind: ConfigMap
metadata:
  name: backplane-zk
  namespace: cecochat
  labels:
    app: backplane-zk
data:
  entrypoint.sh: |-
    #!/bin/bash
    export ZOO_MY_ID=${HOSTNAME##*-}
    /docker-entrypoint.sh zkServer.sh start-foreground
