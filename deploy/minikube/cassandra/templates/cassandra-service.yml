apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}
  namespace: {{.Release.Namespace}}
  labels:
    app: {{.Release.Name}}
spec:
  selector:
    app: {{.Release.Name}}
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{.Values.Networking.CqlPort}}
